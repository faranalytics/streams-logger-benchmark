{
  "name": "streams-logger-benchmark",
  "version": "1.0.0",
  "main": "./dist/index.js",
  "scripts": {
    "clean:build:streams": "rm -rf ./test/streams/dist/* && tsc --build ./tests/streams",
    "clean:build:winston": "rm -rf ./test/winston/dist/* && tsc --build ./tests/winston",
    "clean:build:pino": "rm -rf ./test/pino/dist/* && tsc --build ./tests/pino",
    "clean:build": "rm -rf ./dist/* && tsc --build .",
    "start": "npm run clean:build && npm run clean:build:streams && npm run clean:build:winston && npm run clean:build:pino && node . "
  },
  "author": "",
  "license": "MIT",
  "description": "",
  "dependencies": {
    "typescript": "^5.5.3"
  },
  "devDependencies": {
    "@types/node": "^20.14.10"
  },
  "type": "module"
}