{
  "name": "streams-logger-benchmark",
  "version": "1.0.0",
  "main": "./dist/index.js",
  "scripts": {
    "build:streams": "rm -rf ./test/streams/dist/* && tsc --build ./tests/streams",
    "build:winston": "rm -rf ./test/winston/dist/* && tsc --build ./tests/winston",
    "build:pino": "rm -rf ./test/pino/dist/* && tsc --build ./tests/pino",
    "build": "rm -rf ./dist/* && tsc --build .",
    "start": "npm run build && npm run build:streams && npm run build:winston && npm run build:pino && node . "
  },
  "author": "",
  "license": "MIT",
  "description": "",
  "dependencies": {
    "typescript": "^5.5.3"
  },
  "devDependencies": {
    "@types/node": "^20.14.10"
  },
  "type": "module"
}